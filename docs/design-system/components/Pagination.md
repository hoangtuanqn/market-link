# Pagination

Pagination for long tables and lists.

- The consumer passes `page`, `pages`, `onChange(n)`. It collapses with "…" automatically. The current page uses `aria-current="page"`.

Preview: [reference gallery](../reference/gallery.html#c-Pagination)
